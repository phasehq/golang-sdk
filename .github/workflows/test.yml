name: Go Tests

on:
  push:
    branches: [ main ]
  pull_request:
  release:
    types: [ created ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.25', '1.26']
    env:
      # Fail loudly if go.mod ever requires a newer toolchain than the matrix
      # provides, instead of silently auto-downloading it.
      GOTOOLCHAIN: local
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Vet
        run: go vet ./...

      - name: Run tests
        run: go test -race ./... -v
